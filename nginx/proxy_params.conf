# Shared proxy helpers loaded by the main nginx.conf

# Map for sane WebSocket/upgrade Connection header handling:
# - If the client sends an Upgrade header, pass "Upgrade" as Connection.
# - Otherwise, close the hop-by-hop Connection header (safe for proxying).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
